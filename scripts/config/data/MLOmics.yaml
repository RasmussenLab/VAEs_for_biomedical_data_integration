# DO NOT EDIT DEFAULTS

defaults:
  - base_data

# FEEL FREE TO EDIT BELOW

raw_data_path: MLOmics_Clustering_LUAD_aligned/              # where raw data is stored
interim_data_path: interim_data_MLOmics/  # where intermediate files will be stored
results_path: results_MLOmics/     # where result files will be placed

sample_names: LUAD.ids  # names/IDs of each sample, must appear in the
                                # other datasets

categorical_inputs: []

continuous_inputs:   # a list of continuous datasets
  - name: LUAD_CNV_aligned_MOVE
    log2: false
    scale: true
  - name: LUAD_Methy_aligned_MOVE
    log2: false
    scale: true
  - name: LUAD_miRNA_aligned_MOVE
    log2: false
    scale: true
  - name: LUAD_mRNA_aligned_MOVE
    log2: false
    scale: true